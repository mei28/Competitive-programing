from collections import Counter
from itertools import product
n,x = map(int,input().split())

AA = []
L = []
for i in range(n):
    tmp = list(map(int,input().split()))
    l = tmp[0]
    a = tmp[1:]
    L.append(l)
    AA.append(Counter(a))

ans = 0

l_p = list(itertools.product([a for a in AA]))
